{
  "domain": "{{ domain  | default('consul') }}",
  "advertise_addr": "{{ hive_private_ip }}",
  "retry_join": {{ webgate_join | to_json }},
  "data_dir": "/var/consul",
  "server": true,
  "log_level": "INFO",
  "enable_script_checks" : true,
  "bootstrap_expect": {{ (webgate_join | length) + 1 }}
}
